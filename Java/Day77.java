package Java;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class Day77 {

    // public static void plusMinus(List<Integer> arr) {
    //     double plus = 0, minus = 0, zero = 0;
    //     for (int i : arr) {
    //         if (i > 0)
    //             plus++;
    //         else if (i < 0)
    //             minus++;
    //         else
    //             zero++;
    //     }
    //     double pr = plus / (arr.size());
    //     double mr = minus / (arr.size());
    //     double zr = zero / (arr.size());

    //     System.out.printf("%.6f%n", pr);
    //     System.out.printf("%.6f%n", mr);
    //     System.out.printf("%.6f%n", zr);

    // }

    // public static void main(String[] args) {
    //     List<Integer> integers = new ArrayList<Integer>(Arrays.asList(-4, 3, -9, 0, 4, 1));
    //     plusMinus(integers);
    // }

}

